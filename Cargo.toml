[package]
name = "siphash-napi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "3", features = ["napi6"] }
napi-derive = "3"
siphasher = "1"

[build-dependencies]
napi-build = "2"

[profile.release]
lto = true
strip = "symbols"
